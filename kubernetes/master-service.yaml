apiVersion: v1
kind: Service
metadata:
  name: master
  labels:
    app: master
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: master
  selector:
    app: master
